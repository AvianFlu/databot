#!/usr/bin/env node

var databot = require('../lib/databot');

var config = {
  server : "irc.freenode.net",
  nick : "WallyDater",
  userName : "databot",
  realName : "DateUhBawt",
  channels : [ "#kohai" ]
};

databot.start(config);